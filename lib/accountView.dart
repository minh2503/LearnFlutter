import 'package:flutter/material.dart';

class Accountview extends StatelessWidget {
  const Accountview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Image.asset("assets/image/logo.jpg")],
      ),
    );
  }
}
